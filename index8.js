const friends = ["Umais", "Ramish", "Mohid", "Uzair", "Ayan"];
// used a loop instead of printing each name one by one to keep the code DRY
for (let i = 0; i < friends.length; i++) {
  console.log(friends[i]);
}
